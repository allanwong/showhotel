Ext.define('app.ux.field.ToggleBool', {
    override: 'Ext.field.Toggle',

    getValue: function (config) {
        var value = this.callParent(arguments);
        return value === 1 ? true : false;
    }
});